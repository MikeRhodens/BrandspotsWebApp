<?php

$sorttype = isset($_GET['sort']) ? $_GET['sort'] : '';
$page = isset($_GET['action']) ? $_GET['action'] : '';
$alreadysorted = false;

if($page == 'overzicht'){
  if($sorttype == 'price'){
  }
}




?>
